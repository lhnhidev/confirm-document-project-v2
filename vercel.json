{
  "buildCommand": "npm run build --workspace=apps/frontend/web",
  "outputDirectory": "apps/frontend/web/dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
